#

This folder is used to release plugins via providing links to download.
This folder contains other folders named like "v4", "v5", etc... where "vX" is a major version of the plugin
development kit (PDK).
